# Optional: override the API base URL for production builds.
# Leave unset for local development — the Vite dev server proxies /api
# to http://localhost:5000 automatically.
# VITE_API_URL=https://your-api-domain.com/api
